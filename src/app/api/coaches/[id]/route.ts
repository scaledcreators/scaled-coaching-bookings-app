import { coachInput } from "../route";
import { requireRequestViewer } from "@/lib/auth";
import { getSupabaseAdmin } from "@/lib/supabase";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const input = coachInput.parse(await request.json()); await requireRequestViewer(request, input.companyId, true); const { data, error } = await getSupabaseAdmin().from("coaches").update({ name: input.name, bio: input.bio || null, timezone: input.timezone, updated_at: new Date().toISOString() }).eq("id", id).eq("whop_company_id", input.companyId).select("*").single(); if (error) throw error; return Response.json({ coach: data }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Could not update coach." }, { status: 400 }); } }
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const companyId = new URL(request.url).searchParams.get("companyId"); if (!companyId) throw new Error("companyId is required."); await requireRequestViewer(request, companyId, true); const { error } = await getSupabaseAdmin().from("coaches").update({ status: "archived", updated_at: new Date().toISOString() }).eq("id", id).eq("whop_company_id", companyId); if (error) throw error; return new Response(null, { status: 204 }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Could not archive coach." }, { status: 400 }); } }
