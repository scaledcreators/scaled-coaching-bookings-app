"use client";
import { useState } from "react";
import { Pencil, Plus, Trash2, UserRound, X } from "lucide-react";
import type { Coach } from "@/lib/types";
const empty = { name: "", bio: "", timezone: "America/Chicago" };
export function CoachManager({ companyId, demo, initialCoaches }: { companyId: string; demo: boolean; initialCoaches: Coach[] }) { const [coaches, setCoaches] = useState(initialCoaches); const [editingId, setEditingId] = useState<string | null>(null); const [open, setOpen] = useState(false); const [form, setForm] = useState(empty); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); function edit(coach?: Coach) { setEditingId(coach?.id ?? null); setForm(coach ? { name: coach.name, bio: coach.bio ?? "", timezone: coach.timezone } : empty); setOpen(true); } async function submit(event: React.FormEvent) { event.preventDefault(); setSaving(true); setError(""); try { let coach: Coach; if (demo) coach = { id: editingId ?? crypto.randomUUID(), whop_company_id: companyId, ...form, status: "active" }; else { const response = await fetch(editingId ? `/api/coaches/${editingId}` : "/api/coaches", { method: editingId ? "PATCH" : "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ companyId, ...form }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error); coach = payload.coach; } setCoaches((items) => editingId ? items.map((item) => item.id === editingId ? coach : item) : [...items, coach]); setOpen(false); } catch (reason) { setError(reason instanceof Error ? reason.message : "Could not save coach."); } finally { setSaving(false); } } async function archive(id: string) { if (!demo) { const response = await fetch(`/api/coaches/${id}?companyId=${encodeURIComponent(companyId)}`, { method: "DELETE" }); if (!response.ok) return; } setCoaches((items) => items.filter((item) => item.id !== id)); } return <div className="content-stack fade-in"><section className="unavailable-hero"><div><p className="eyebrow">Your team</p><h2>Coaches & assignment</h2><p>Manage the people who deliver sessions. Each coach receives their own weekly availability.</p></div><button className="sc-btn-primary" onClick={() => edit()}><Plus size={16}/> Add coach</button></section><div className="coach-grid">{coaches.map((coach) => <article className="coach-card panel" key={coach.id}><span className="coach-avatar"><UserRound/></span><div><span className="health-badge success">Active</span><h3>{coach.name}</h3><p>{coach.bio || "No bio yet."}</p><small>{coach.timezone}</small><div className="manage-actions"><button onClick={() => edit(coach)}><Pencil size={14}/> Edit</button><button className="danger-link" onClick={() => archive(coach.id)}><Trash2 size={14}/> Archive</button></div></div></article>)}</div>{open && <div className="modal-backdrop"><form className="modal sc-card" onSubmit={submit}><div className="panel-heading"><div><p className="eyebrow">Team member</p><h2>{editingId ? "Edit coach" : "Add a coach"}</h2></div><button type="button" className="icon-button" onClick={() => setOpen(false)}><X size={18}/></button></div><div className="field"><label>Name</label><input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} required/></div><div className="field"><label>Bio / specialty</label><textarea value={form.bio} onChange={(event) => setForm({ ...form, bio: event.target.value })}/></div><div className="field"><label>Timezone</label><input value={form.timezone} onChange={(event) => setForm({ ...form, timezone: event.target.value })} required/></div>{error && <p className="form-error">{error}</p>}<div className="modal-actions"><button type="button" className="sc-btn-secondary" onClick={() => setOpen(false)}>Cancel</button><button className="sc-btn-primary" disabled={saving}>{saving ? "Saving…" : "Save coach"}</button></div></form></div>}</div>; }
